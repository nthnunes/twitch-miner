[tool.semantic_release]
version_variables = ["_version.py:__version__"]
build_command = ""
commit_message = "chore(release): v{version} [skip ci]"
tag_format = "v{version}"

[tool.semantic_release.branches.main]
match = "main"
